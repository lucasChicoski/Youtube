import 'package:flutter/material.dart';

class Libraries extends StatelessWidget {
  

  @override
  Widget build(BuildContext context) {
    return Center(
      child: Text("Biblioteca", style: TextStyle(fontSize: 40),),
    );
  }
}